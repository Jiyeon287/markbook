<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.markbook.mapper.booktestMapper">

	<insert id="answerOne1">
	insert into booktest 
	(economy, it, history, novel, essay, travel) 
	values (1,1,1,0,0,0)
	</insert>
	
	<insert id="answerOne2">
	insert into booktest 
	(novel, essay, travel, economy, it, history) 
	values (3,3,3,0,0,0)
	</insert>
	
	<select id="answerTwo" resultType="int">
	select max(testnum) from booktest
	</select>
	
	<update id="answerTwo1">
	update booktest
	set economy = economy + 15   
	where testnum=#{testnum}
	</update>
	
	<update id="answerTwo2">
	update booktest
	set novel = novel +1
	where testnum=#{testnum}
	</update>
	
	<update id="answerTwo3">
	update booktest
	set travel = travel + 5, essay = essay + 5 
	where testnum=#{testnum}
	</update>
	
	<update id="answerTwo4">
	update booktest
	set it = it + 30, history = history + 30
	where testnum=#{testnum}
	</update>

	
 </mapper>